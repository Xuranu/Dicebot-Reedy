# Basic DiceBot

Download all these files and put them in a single directory.
Create an account on Heroku. You'll need to read some tutorials
on how to do that.

You'll also need to set a value for `SLACK_API_TOKEN` using the
`heroku config set` command.